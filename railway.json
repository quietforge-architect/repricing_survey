{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "gunicorn --bind 0.0.0.0:$PORT agents.logic.sqlite_service.index:app",
    "healthcheckPath": "/api/health"
  }
}